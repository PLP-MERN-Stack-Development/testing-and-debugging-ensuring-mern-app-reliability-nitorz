const request = require('supertest'); const express = require('express'); const userRoutes = require('../../src/routes/user'); const app = express(); app.use('/api/user', userRoutes); test('GET /api/user/hello returns Hello World', async () => { const res = await request(app).get('/api/user/hello'); expect(res.statusCode).toBe(200); expect(res.body.message).toBe('Hello World'); });